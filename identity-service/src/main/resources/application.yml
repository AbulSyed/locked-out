server:
  port: 8081

spring:
  application:
    name: identity-service
  datasource:
    url:
    username:
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  version: v202306291521
