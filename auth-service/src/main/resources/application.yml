server:
  port: 8080

spring:
  application:
    name: auth-service

app:
  version: v202605171128

feign:
  client:
    config:
      identityService:
        url: ${IDENTITY_SERVICE_URL:http://localhost:8081}
