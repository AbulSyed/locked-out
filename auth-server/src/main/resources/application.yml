server:
  port: 8080

spring:
  application:
    name: auth-server
  datasource:
    url:
    username:
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  version: v20230628